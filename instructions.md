# Easey’s site — quick reference

The living **build + content map** is in [`README.md`](./README.md).

- **App routes:** `src/app/`
- **UI:** `src/components/`
- **Tokens + Tailwind theme:** `src/styles/globals.css`
- **Copy + menu seed:** `src/content/`

Legacy URLs (`/menu`, `/gift-cards`, old location pages, etc.) redirect in `next.config.ts`.
